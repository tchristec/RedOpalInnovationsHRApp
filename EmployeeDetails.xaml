<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RedOpalInnovationsHRApp.EmployeeDetails"
             Title="EmployeeDetails">
    <StackLayout>
        <Frame BackgroundColor="Transparent">
            <StackLayout>
                <!-- Display Employee Details Here -->
                <Label Text="{Binding FirstName}" />
                <Label Text="{Binding LastName}" />
                <Label Text="{Binding PhoneNumber}" />
                <Label Text="{Binding Department}" />
                <Label Text="{Binding StreetAddress}" />
                <Label Text="{Binding CityAddress}" />
                <Label Text="{Binding StateAddress}" />
                <Label Text="{Binding PostalCodeAddress}" />
                <Label Text="{Binding CountryAddress}" />
            </StackLayout>
        </Frame>
    </StackLayout>
</ContentPage>